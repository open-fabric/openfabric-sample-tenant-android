pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        mavenLocal() // <-- add mavenLocal as one of the repositories
        google()
        mavenCentral()
    }
}
rootProject.name = "openfabric-sample-tenant-android"
include ':app'
include ':openfabric-unilateral-sdk'
