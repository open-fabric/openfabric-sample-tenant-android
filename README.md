# Unilateral SDK Sample

## Getting started

Update your gradle.properties, add the following lines to the bottom of the
file:

```ini
gpr.user=api-openfabric
gpr.key=<token>
```

Replace `<token>` with the value provided by Open Fabric.
